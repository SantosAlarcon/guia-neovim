---

---

<section class="offcanvas__container"></section>

<style>
    .offcanvas__container {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-color: rgba(0, 0, 0, 0.5);
        backdrop-filter: blur(.5rem);
        z-index: 9;
        opacity: 0;
        visibility: hidden;
        transition: opacity .3s ease-in-out, visibility .3s ease-in-out;

        &[show] {
            opacity: 1;
            visibility: visible;
        }
    }
</style>
